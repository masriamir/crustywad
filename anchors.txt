# Living-docs anchor strings (ADR-0007).
#
# Each non-empty, non-comment line is a substring that must appear verbatim in
# ALL of the checked documentation files:
#
#   AGENTS.md
#   docs/design.md
#
# Run the detector locally with:
#
#   just docs-sync
#
# When renaming a convention phrase, update:
#   1. The documentation files above — but a phrase inside a synced `meta:` block in
#      AGENTS.md must be changed at its canonical source in masriamir/.github and synced
#      in (never edited in AGENTS.md directly); only repo-authored phrases are edited locally
#   2. This file (anchors.txt)
#
# ─── Core conventions ────────────────────────────────────────────────────────

# Project identity
MSRV 1.94.0
MIT OR Apache-2.0

# Error handling
thiserror

# Documentation
missing_docs

# Safety
deny(unsafe_code)

# Lints
clippy::pedantic

# Parsing API
ParseOptions { strictness

# Testing
proptest

# Workflow
just ci
Conventional Commits
